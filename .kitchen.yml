---
driver:
  name: vagrant

provisioner:
  name: chef_zero
  always_update_cookbooks: true

platforms:
  - name: ubuntu-16.04
suites:
  - name: project
    run_list:
      - recipe[project::operating_system]
      - recipe[project::nginx]
    attributes:
